# ============================================================
# SSD 지능형 결함 분석 시스템 (OpenAI 버전) - Python 의존성
# ============================================================

# --- 웹 프레임워크 ---
fastapi==0.111.0          # FastAPI 비동기 웹 프레임워크
uvicorn[standard]==0.29.0 # ASGI 서버

# --- DB 연결 ---
PyMySQL==1.1.0            # MariaDB / MySQL 순수 파이썬 드라이버

# --- AI / LLM 프레임워크 ---
langchain==0.1.20         # LangChain 코어 (LCEL 파이프라인 포함)
langchain-openai==0.1.8   # ChatOpenAI, OpenAIEmbeddings 공식 통합 패키지
langchain-community==0.0.38  # Chroma 등 커뮤니티 인테그레이션
langchain-chroma==0.1.1   # Chroma 벡터 스토어 LangChain 래퍼

# --- 벡터 DB ---
chromadb==0.5.0           # Chroma 로컬 벡터 데이터베이스

# --- 환경 변수 / 유틸리티 ---
python-dotenv==1.0.1      # .env 파일로부터 환경 변수 로드
pydantic==2.7.1           # 데이터 검증 (FastAPI 내부 의존)

# --- OpenAI SDK (langchain-openai 의 전이 의존성이지만 명시 권장) ---
openai==1.30.5            # OpenAI Python SDK
